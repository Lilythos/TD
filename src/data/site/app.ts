import { ref } from 'vue'

export const apps = ref([
  {
    name: 'Apps',
    autoLoad: true,
    description: 'Static apps list',
    manualItems: [
      // Add your apps here
    ],
  },
])

export default apps
